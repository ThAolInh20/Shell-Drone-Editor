export const editorConfig = {
  gizmo: {
    // Step sizes for keyboard shortcuts (Ctrl + Arrow)
    keyboardTranslateStep: 1.0,  // in meters / grid units (default: 1.0)
    keyboardScaleStep: 1.10,     // multiplier factor (default: 1.10 = +10% size)
    keyboardRotateStep: 15,      // in degrees (default: 15)

    // Step size for UI inputs (Scale X, Y, Z in properties panels)
    uiScaleInputStep: 0.05       // default: 0.05
  }
};
