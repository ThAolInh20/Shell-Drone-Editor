{
  "name": "shell-drone-animation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && electron .\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^42.2.0",
    "electron-is-dev": "^3.0.1",
    "vite": "^8.0.4",
    "wait-on": "^9.0.10"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}